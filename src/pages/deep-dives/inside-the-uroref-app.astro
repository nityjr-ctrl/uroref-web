---
import Base from '../../layouts/Base.astro';
---
<Base
  title="Inside the UroRef App"
  description="The on-call companion for urology trainees — decision trees, procedure guides, calculators, drug doses, clinic frameworks and BAUS patient information, all in one place."
>

<!-- ─────────────────────────────────────────────────────────────
     1. HERO
     ───────────────────────────────────────────────────────────── -->
<section class="ia-hero u-hero">
  <div class="u-hero-bg" aria-hidden="true"></div>
  <div class="u-hero-grid" aria-hidden="true"></div>

  <div class="ia-hero-inner">

    <a href="/deep-dives" class="ia-back">← Deep Dives</a>

    <span class="u-eyebrow-pill">
      <span class="u-live-dot"></span>
      Inside the App
    </span>

    <h1 class="ia-title u-gradient-text">Inside the UroRef App</h1>

    <blockquote class="ia-quote">
      <div class="ia-quote-mark" aria-hidden="true">"</div>
      <p>Sometimes someone suddenly asks you, "What's monopolar versus bipolar?" and your brain freezes. The answer is in the name — bipolar means two poles within the device, while monopolar means one pole in the device and the other on the patient — but under pressure, even simple knowledge can feel briefly out of reach.</p>
      <cite>Personal experience</cite>
    </blockquote>

    <p class="ia-intro">That feeling is a large part of why UroRef exists. Not because the knowledge is not there, but because access to it can become harder in the moment — especially on call, when you are tired, interrupted, and thinking about five things at once. UroRef was built to make important information easier to reach, easier to scan, and easier to follow.</p>

    <p class="ia-summary">The on-call companion for urology trainees — decision trees, procedure guides, calculators, drug doses, clinic frameworks, and BAUS patient information, all organised into one practical, accessible space.</p>

    <div class="ia-stats">
      <div class="ia-stat">
        <span class="ia-stat-num">500+</span>
        <span class="ia-stat-lbl">trainees using the app</span>
      </div>
      <div class="ia-stat">
        <span class="ia-stat-num">16</span>
        <span class="ia-stat-lbl">on-call pathways</span>
      </div>
      <div class="ia-stat">
        <span class="ia-stat-num">31</span>
        <span class="ia-stat-lbl">patient info leaflets</span>
      </div>
    </div>

    <div class="ia-cta-row">
      <a href="/app" class="u-cta-primary">
        Get the app
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
      </a>
      <a href="#features" class="u-cta-ghost">See what's inside</a>
    </div>

  </div>
</section>


<!-- ─────────────────────────────────────────────────────────────
     2. WHY IT'S BUILT THIS WAY
     ───────────────────────────────────────────────────────────── -->
<section class="ia-section ia-section--dark">
  <div class="ia-narrow">
    <p class="ia-eyebrow">Why it's built this way</p>
    <h2>Built around how I think and work</h2>
    <div class="ia-prose">
      <p>UroRef was built around the way I think and work in practice. I find it much easier when problems are broken down clearly, slowly, and step by step — especially on call when your attention is split, the bleep is going off, and you are trying to make safe decisions quickly. That is the structure I wanted the app to follow.</p>
      <p>In many ways, UroRef works for me because this is how my brain works. I do not find long walls of guidance or scattered resources especially helpful in the moment. What helps me is a clear sequence, a simple framework, and a layout that makes the next step easy to find. That is why the app is designed to feel practical, structured, and accessible under pressure.</p>
      <p>The whole point is not to create new urology knowledge. The point is to organise useful information into one place and present it in a format that is quick to navigate, easy to scan, and more dyslexia-friendly in the way it is laid out.</p>
    </div>
  </div>
</section>


<!-- ─────────────────────────────────────────────────────────────
     3. THE PROBLEM IT SOLVES
     ───────────────────────────────────────────────────────────── -->
<section class="ia-section">
  <div class="ia-wide">
    <p class="ia-eyebrow">The problem it solves</p>
    <h2>The information exists. Access is the problem.</h2>
    <p class="ia-section-intro">Useful guidance is already out there. The issue is finding it quickly — under pressure, at 2am, on a ward with poor Wi-Fi. That is what UroRef is designed to solve.</p>

    <div class="ia-problem-grid">

      <div class="ia-problem-card">
        <div class="ia-problem-icon" aria-hidden="true">🌙</div>
        <h3>When you're stuck at 2am</h3>
        <p>The bleep is going. The patient is waiting. You don't have time to scroll through a PDF or open multiple tabs. UroRef puts the answer one tap away, every time.</p>
      </div>

      <div class="ia-problem-card">
        <div class="ia-problem-icon" aria-hidden="true">📋</div>
        <h3>When you need steps, not theory</h3>
        <p>Knowing <em>about</em> a procedure is different from knowing what to do next. UroRef gives you clear, sequential steps — not background reading.</p>
      </div>

      <div class="ia-problem-card">
        <div class="ia-problem-icon" aria-hidden="true">📵</div>
        <h3>When signal or Wi-Fi is poor</h3>
        <p>Wards, theatres, corridors — the places where you need this most are the same places where internet access disappears. UroRef works fully offline.</p>
      </div>

    </div>
  </div>
</section>


<!-- ─────────────────────────────────────────────────────────────
     4. ON-CALL DECISION TREES  (visually prominent)
     ───────────────────────────────────────────────────────────── -->
<section class="ia-decision">
  <div class="ia-wide">

    <div class="ia-decision-lead">
      <p class="ia-eyebrow ia-eyebrow--teal">The heart of the app</p>
      <h2>On-call decision trees</h2>
      <p class="ia-decision-intro">Each tree is built around the question you're actually asking at 2am — not a textbook walkthrough, but a step-by-step path that cuts through cognitive clutter and ends with a clear action. No theory. Just: <em>this finding → this action.</em></p>
    </div>

    <div class="ia-condition-grid">

      <div class="ia-condition ia-condition--red">
        <div class="ia-condition-name">Obstructed infected kidney</div>
        <div class="ia-condition-sub">Resus now → decompress within hours</div>
      </div>

      <div class="ia-condition ia-condition--red">
        <div class="ia-condition-name">Priapism</div>
        <div class="ia-condition-sub">Ischaemic vs non-ischaemic pathway</div>
      </div>

      <div class="ia-condition ia-condition--amber">
        <div class="ia-condition-name">Clot retention</div>
        <div class="ia-condition-sub">3-way catheter · CBI · theatre if fails</div>
      </div>

      <div class="ia-condition ia-condition--amber">
        <div class="ia-condition-name">Acute scrotum</div>
        <div class="ia-condition-sub">Torsion until proven otherwise</div>
      </div>

      <div class="ia-condition ia-condition--teal">
        <div class="ia-condition-name">Difficult catheterisation</div>
        <div class="ia-condition-sub">Step-by-step decision tree</div>
      </div>

      <div class="ia-condition ia-condition--teal">
        <div class="ia-condition-name">Haematuria work-up</div>
        <div class="ia-condition-sub">Visible vs non-visible, initial approach</div>
      </div>

    </div>
  </div>
</section>


<!-- ─────────────────────────────────────────────────────────────
     5. FEATURE BUCKETS
     ───────────────────────────────────────────────────────────── -->
<section id="features" class="ia-section">
  <div class="ia-wide">
    <p class="ia-eyebrow">What's inside</p>
    <h2>Four areas, one place</h2>

    <div class="ia-feature-grid">

      <div class="ia-feature">
        <div class="ia-feature-head">
          <span class="ia-feature-icon" aria-hidden="true">🚨</span>
          <h3>Emergency support</h3>
        </div>
        <p>Decision trees for the calls that don't wait. Each pathway walks you through the clinical decision step by step, ending with a clear action rather than a differential list.</p>
        <ul>
          <li>On-call decision trees</li>
          <li>Urgent pathway checklists</li>
          <li>Sepsis recognition prompts</li>
          <li>When to escalate to your consultant</li>
        </ul>
      </div>

      <div class="ia-feature">
        <div class="ia-feature-head">
          <span class="ia-feature-icon" aria-hidden="true">🔧</span>
          <h3>Procedure support</h3>
        </div>
        <p>16 procedure guides built for practical use — structured as both operative note templates and step-by-step technical guides, covering the steps that matter and the things that go wrong.</p>
        <ul>
          <li>Flexible and rigid cystoscopy</li>
          <li>JJ stent insertion &amp; nephrostomy</li>
          <li>Suprapubic catheter &amp; TWOC</li>
          <li>Scrotal and penile procedures</li>
        </ul>
      </div>

      <div class="ia-feature">
        <div class="ia-feature-head">
          <span class="ia-feature-icon" aria-hidden="true">🏥</span>
          <h3>Clinic support</h3>
        </div>
        <p>Structured frameworks for common outpatient presentations, plus the full BAUS patient information leaflet suite — useful before consent conversations and in the middle of a busy clinic.</p>
        <ul>
          <li>New haematuria clinic framework</li>
          <li>LUTS and BPH structure</li>
          <li>Post-operative follow-up guides</li>
          <li>31 BAUS patient leaflets</li>
        </ul>
      </div>

      <div class="ia-feature">
        <div class="ia-feature-head">
          <span class="ia-feature-icon" aria-hidden="true">📲</span>
          <h3>Offline-first</h3>
        </div>
        <p>Everything works without a data connection. Because the wards, theatres, and corridors where you need this most are the same places where hospital Wi-Fi reliably disappears.</p>
        <ul>
          <li>Full offline access</li>
          <li>Drug dose reference</li>
          <li>Calculators (BOOI, BCI, stone)</li>
          <li>Works anywhere on the ward</li>
        </ul>
      </div>

    </div>
  </div>
</section>


<!-- ─────────────────────────────────────────────────────────────
     6. SOURCES & ACKNOWLEDGEMENTS
     ───────────────────────────────────────────────────────────── -->
<section class="ia-section ia-section--dark">
  <div class="ia-narrow">
    <p class="ia-eyebrow">Sources &amp; acknowledgements</p>
    <h2>Where the content comes from</h2>

    <div class="ia-prose">
      <p>Most of the content in UroRef is not original material written by me. It is organised and formatted from established sources that already exist. The value of UroRef is not in new knowledge — it is in presentation, organisation, and accessibility.</p>
      <p>The only sections that come partly from my own personal notes are the on-call step-by-step guides and parts of the operative note sections. Even these are practical summaries based on clinical experience and standard teaching — not replacements for formal guidance, local protocols, or senior advice.</p>
    </div>

    <div class="ia-sources-box">
      <div class="ia-sources-label">Content is drawn from</div>
      <ul class="ia-sources-list">
        <li>EAU Guidelines</li>
        <li>NICE guidance</li>
        <li>BAUS patient information leaflets</li>
        <li>Standard urology reference material and textbooks</li>
        <li>Selected practical summaries for on-call and operative use</li>
      </ul>
    </div>

    <p class="ia-sources-close">UroRef does not pretend to invent new knowledge. The goal is to make existing, trusted information easier to reach, easier to read, and more accessible under pressure — particularly for trainees who find dense or scattered resources harder to use.</p>
  </div>
</section>


<!-- ─────────────────────────────────────────────────────────────
     7. AVAILABILITY CTA
     ───────────────────────────────────────────────────────────── -->
<section class="ia-avail">
  <div class="ia-avail-inner">
    <p class="ia-avail-eyebrow">Available now</p>
    <h2>UroRef is live on TestFlight</h2>
    <p>Over 500 UK urology trainees are already using it. Android and public App Store releases are coming. The app is free — join the newsletter to get the TestFlight link straight to your inbox.</p>
    <div class="ia-cta-row">
      <a href="/app" class="u-cta-primary">
        Get the app
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
      </a>
      <a href="https://forms.gle/4QTK3MSgMsedJQEw7" target="_blank" rel="noopener" class="u-cta-ghost">Join the newsletter</a>
    </div>
  </div>
</section>


<!-- ─────────────────────────────────────────────────────────────
     8. FINAL NOTE
     ───────────────────────────────────────────────────────────── -->
<section class="ia-final">
  <div class="ia-final-inner">
    <p>UroRef was not built to add more noise to an already loud environment. It was built because useful information deserves a better home — somewhere calm, practical, and easy to use in the moments that matter most. If it makes one on-call shift a little less overwhelming, it has done its job.</p>
  </div>
</section>

</Base>


<style is:global>
  html { scroll-behavior: smooth; }
</style>

<style>
/* ── Shared layout helpers ─────────────────────────────────── */
.ia-narrow {
  max-width: 760px;
  margin: 0 auto;
  padding: 88px 32px;
}
.ia-wide {
  max-width: var(--container);
  margin: 0 auto;
  padding: 88px 32px;
}

/* ── Eyebrow label ─────────────────────────────────────────── */
.ia-eyebrow {
  font-family: var(--font-mono);
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--teal-500);
  margin: 0 0 18px;
  display: flex;
  align-items: center;
  gap: 10px;
}
.ia-eyebrow::before {
  content: '';
  display: inline-block;
  width: 18px;
  height: 1px;
  background: currentColor;
  opacity: 0.5;
  flex-shrink: 0;
}
.ia-eyebrow--teal { color: rgba(0,212,200,0.85); }

/* ── Section wrapper ───────────────────────────────────────── */
.ia-section { border-top: 1px solid var(--border); }
.ia-section--dark {
  background: var(--navy-900);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}

/* Section h2 */
.ia-section h2,
.ia-section--dark h2 {
  font-size: clamp(1.65rem, 4vw, 2.25rem);
  font-weight: 600;
  letter-spacing: -0.025em;
  line-height: 1.15;
  margin: 0 0 24px;
}
.ia-section-intro {
  font-family: var(--font-serif);
  font-size: clamp(1rem, 2vw, 1.06rem);
  color: var(--text-secondary);
  line-height: 1.78;
  max-width: 64ch;
  margin: 0 0 52px;
}

/* Prose blocks (sections 2 + 6) */
.ia-prose p {
  font-family: var(--font-serif);
  font-size: clamp(1rem, 2vw, 1.06rem);
  color: var(--text-secondary);
  line-height: 1.82;
  max-width: 66ch;
  margin: 0 0 26px;
}
.ia-prose p:last-child { margin-bottom: 0; }


/* ─────────────────────────────────────────────────────────────
   1. HERO
   ───────────────────────────────────────────────────────────── */
.ia-hero { padding: 0; }

.ia-hero-inner {
  max-width: var(--container);
  margin: 0 auto;
  padding: 80px 32px 96px;
  position: relative;
}

.ia-back {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  color: var(--text-muted);
  font-size: 13px;
  margin-bottom: 28px;
  transition: color 0.15s;
}
.ia-back:hover { color: var(--text-primary); }

.ia-title {
  font-size: clamp(2.4rem, 6vw, 4.25rem);
  font-weight: 700;
  letter-spacing: -0.04em;
  line-height: 1.05;
  margin: 22px 0 52px;
  max-width: 14ch;
}

/* Quote */
.ia-quote {
  margin: 0 0 44px;
  padding: 32px 36px 28px;
  background: linear-gradient(135deg, rgba(0,212,200,0.07) 0%, rgba(5,15,28,0.5) 100%);
  border: 1px solid rgba(0,212,200,0.18);
  border-left: 3px solid var(--teal-500);
  border-radius: 0 var(--r-2xl) var(--r-2xl) 0;
  max-width: 700px;
}
.ia-quote-mark {
  font-family: var(--font-serif);
  font-size: 4.5rem;
  line-height: 0.55;
  color: var(--teal-500);
  opacity: 0.35;
  display: block;
  margin-bottom: 16px;
  user-select: none;
}
.ia-quote p {
  font-family: var(--font-serif);
  font-size: clamp(1.08rem, 2.5vw, 1.28rem);
  color: var(--text-primary);
  line-height: 1.72;
  margin: 0 0 18px;
  font-style: normal;
}
.ia-quote cite {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--text-muted);
  font-style: italic;
  letter-spacing: 0.04em;
  display: block;
}

.ia-intro {
  font-family: var(--font-serif);
  font-size: clamp(1rem, 2vw, 1.1rem);
  color: var(--text-secondary);
  line-height: 1.78;
  max-width: 65ch;
  margin: 0 0 18px;
}
.ia-summary {
  font-family: var(--font-sans);
  font-size: clamp(0.9rem, 1.8vw, 0.98rem);
  color: var(--text-muted);
  line-height: 1.65;
  max-width: 65ch;
  margin: 0 0 52px;
}

/* Stats */
.ia-stats {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 8px 0;
  margin-bottom: 44px;
}
.ia-stat {
  display: flex;
  flex-direction: column;
  padding-right: 32px;
  margin-right: 32px;
  border-right: 1px solid var(--border);
}
.ia-stat:last-child {
  border-right: none;
  margin-right: 0;
  padding-right: 0;
}
.ia-stat-num {
  font-size: clamp(1.75rem, 4vw, 2.25rem);
  font-weight: 700;
  letter-spacing: -0.045em;
  color: var(--teal-500);
  line-height: 1;
}
.ia-stat-lbl {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-top: 7px;
}

.ia-cta-row {
  display: flex;
  gap: 14px;
  flex-wrap: wrap;
  align-items: center;
}


/* ─────────────────────────────────────────────────────────────
   3. PROBLEM CARDS
   ───────────────────────────────────────────────────────────── */
.ia-problem-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}
@media (max-width: 820px) {
  .ia-problem-grid { grid-template-columns: 1fr; }
}

.ia-problem-card {
  background: var(--navy-700);
  border: 1px solid var(--border);
  border-radius: var(--r-xl);
  padding: 28px 28px 32px;
}
.ia-problem-icon {
  font-size: 1.75rem;
  display: block;
  margin-bottom: 18px;
}
.ia-problem-card h3 {
  font-size: 1.02rem;
  font-weight: 600;
  letter-spacing: -0.015em;
  line-height: 1.3;
  margin: 0 0 12px;
}
.ia-problem-card p {
  font-family: var(--font-serif);
  font-size: 0.96rem;
  color: var(--text-secondary);
  line-height: 1.72;
  margin: 0;
}
.ia-problem-card p em { color: var(--text-primary); font-style: italic; }


/* ─────────────────────────────────────────────────────────────
   4. DECISION TREES
   ───────────────────────────────────────────────────────────── */
.ia-decision {
  background:
    radial-gradient(circle at 85% 40%, rgba(0,212,200,0.13) 0%, transparent 55%),
    radial-gradient(circle at 5% 90%, rgba(0,212,200,0.06) 0%, transparent 50%),
    var(--navy-900);
  border-top: 1px solid rgba(0,212,200,0.14);
  border-bottom: 1px solid rgba(0,212,200,0.14);
}
.ia-decision .ia-wide { padding-bottom: 96px; }

.ia-decision-lead {
  max-width: 680px;
  margin-bottom: 52px;
}
.ia-decision h2 {
  font-size: clamp(1.75rem, 4.5vw, 2.75rem);
  font-weight: 700;
  letter-spacing: -0.03em;
  line-height: 1.1;
  margin: 0 0 22px;
}
.ia-decision-intro {
  font-family: var(--font-serif);
  font-size: clamp(1rem, 2vw, 1.08rem);
  color: var(--text-secondary);
  line-height: 1.78;
  max-width: 64ch;
  margin: 0;
}
.ia-decision-intro em { color: var(--teal-500); font-style: italic; }

.ia-condition-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 14px;
}
@media (max-width: 900px)  { .ia-condition-grid { grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 540px)  { .ia-condition-grid { grid-template-columns: 1fr; } }

.ia-condition {
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.07);
  border-left-width: 3px;
  border-radius: var(--r-lg);
  padding: 18px 20px;
  transition: background 0.15s;
}
.ia-condition:hover { background: rgba(255,255,255,0.06); }
.ia-condition--red   { border-left-color: var(--cat-oncall); }
.ia-condition--amber { border-left-color: #E68C1E; }
.ia-condition--teal  { border-left-color: var(--teal-500); }

.ia-condition-name {
  font-family: var(--font-sans);
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--text-primary);
  line-height: 1.3;
  margin-bottom: 6px;
}
.ia-condition-sub {
  font-family: var(--font-mono);
  font-size: 0.72rem;
  color: var(--text-muted);
  line-height: 1.5;
  letter-spacing: 0.02em;
}


/* ─────────────────────────────────────────────────────────────
   5. FEATURE BUCKETS
   ───────────────────────────────────────────────────────────── */
.ia-feature-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
  margin-top: 52px;
}
@media (max-width: 680px) { .ia-feature-grid { grid-template-columns: 1fr; } }

.ia-feature {
  background: var(--navy-700);
  border: 1px solid var(--border);
  border-radius: var(--r-xl);
  padding: 28px 30px 32px;
}
.ia-feature-head {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 16px;
}
.ia-feature-icon { font-size: 1.5rem; flex-shrink: 0; }
.ia-feature h3 {
  font-size: 1.05rem;
  font-weight: 600;
  letter-spacing: -0.015em;
  line-height: 1.25;
  margin: 0;
}
.ia-feature > p {
  font-family: var(--font-serif);
  font-size: 0.95rem;
  color: var(--text-secondary);
  line-height: 1.72;
  margin: 0 0 20px;
}
.ia-feature ul {
  list-style: none;
  padding: 0;
  margin: 0;
  border-top: 1px solid var(--border);
  padding-top: 18px;
  display: flex;
  flex-direction: column;
  gap: 9px;
}
.ia-feature li {
  font-family: var(--font-mono);
  font-size: 0.8rem;
  color: var(--text-muted);
  display: flex;
  align-items: center;
  gap: 10px;
  letter-spacing: 0.01em;
}
.ia-feature li::before {
  content: '';
  width: 4px;
  height: 4px;
  border-radius: 50%;
  background: var(--teal-500);
  opacity: 0.65;
  flex-shrink: 0;
}


/* ─────────────────────────────────────────────────────────────
   6. SOURCES
   ───────────────────────────────────────────────────────────── */
.ia-sources-box {
  margin: 36px 0 28px;
  padding: 24px 28px;
  background: rgba(255,255,255,0.025);
  border: 1px solid var(--border);
  border-radius: var(--r-xl);
}
.ia-sources-label {
  font-family: var(--font-mono);
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.14em;
  color: var(--teal-500);
  margin-bottom: 16px;
}
.ia-sources-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 11px;
}
.ia-sources-list li {
  font-family: var(--font-serif);
  font-size: 0.96rem;
  color: var(--text-secondary);
  display: flex;
  align-items: flex-start;
  gap: 12px;
  line-height: 1.5;
}
.ia-sources-list li::before {
  content: '→';
  color: var(--teal-500);
  font-family: var(--font-mono);
  font-size: 0.78rem;
  flex-shrink: 0;
  margin-top: 2px;
}
.ia-sources-close {
  font-family: var(--font-serif);
  font-size: clamp(0.95rem, 2vw, 1.02rem);
  color: var(--text-muted);
  line-height: 1.78;
  max-width: 66ch;
  font-style: italic;
  margin: 0;
}


/* ─────────────────────────────────────────────────────────────
   7. AVAILABILITY
   ───────────────────────────────────────────────────────────── */
.ia-avail {
  background:
    radial-gradient(circle at 50% 0%, rgba(0,212,200,0.14) 0%, transparent 60%),
    var(--navy-800);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
  padding: 96px 32px;
  text-align: center;
}
.ia-avail-inner {
  max-width: 600px;
  margin: 0 auto;
}
.ia-avail-eyebrow {
  font-family: var(--font-mono);
  font-size: 11px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--teal-500);
  margin-bottom: 20px;
}
.ia-avail h2 {
  font-size: clamp(1.8rem, 4.5vw, 2.6rem);
  font-weight: 700;
  letter-spacing: -0.03em;
  line-height: 1.1;
  margin: 0 0 20px;
}
.ia-avail p {
  font-family: var(--font-serif);
  font-size: 1.05rem;
  color: var(--text-secondary);
  line-height: 1.72;
  max-width: 52ch;
  margin: 0 auto 44px;
}
.ia-avail .ia-cta-row { justify-content: center; }


/* ─────────────────────────────────────────────────────────────
   8. FINAL NOTE
   ───────────────────────────────────────────────────────────── */
.ia-final {
  padding: 72px 32px 80px;
  text-align: center;
}
.ia-final-inner {
  max-width: 560px;
  margin: 0 auto;
}
.ia-final p {
  font-family: var(--font-serif);
  font-size: clamp(1rem, 2vw, 1.1rem);
  color: var(--text-muted);
  line-height: 1.82;
  font-style: italic;
  margin: 0;
}


/* ─────────────────────────────────────────────────────────────
   Mobile
   ───────────────────────────────────────────────────────────── */
@media (max-width: 640px) {
  .ia-narrow,
  .ia-wide          { padding: 56px 20px; }
  .ia-hero-inner    { padding: 52px 20px 64px; }
  .ia-quote         { padding: 24px 22px 22px; }
  .ia-quote-mark    { font-size: 3rem; }
  .ia-decision      { border-radius: 0; }
  .ia-decision .ia-wide { padding: 56px 20px 64px; }
  .ia-avail         { padding: 64px 20px; }
  .ia-final         { padding: 56px 20px 64px; }
  .ia-stat          { padding-right: 22px; margin-right: 22px; }
}
</style>
